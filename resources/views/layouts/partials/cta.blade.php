<section id="call-to-action" class="call-to-action section accent-background">
    <div class="container">
        <div class="row justify-content-center" data-aos="zoom-in" data-aos-delay="100">
            <div class="col-xl-10">
                <div class="text-center">
                    <h2 x-text="translations.messages.cta_title">{{ __('messages.cta_title') }}</h2>
                    <p x-text="translations.messages.cta_description">{{ __('messages.cta_description') }}</p>
                    <a class="cta-btn" href="{{ route('contact') }}" x-text="translations.messages.get_quote">{{ __('messages.get_quote') }}</a>
                </div>
            </div>
        </div>
    </div>
</section>